Open your terminal/command prompt, navigate to your DojoAssignemnts folder created earlier:
	Press Windows key, type "cmd", press Enter
	"cd C:\users\jelwonger\Desktop\DojoAssignments"

Initialize a git repository in your DojoAssignments directory:
	"git init"

Add, commit, and push these changes to GitHub:
	"git add ."
	"git commit -m "First Commit, adding folders and about.txt files""
	"git push --set-upstream origin master"

Do a status check to see the changes amde, then add, commit and push your changes:
	"git status"
	"git add ."
	"git commit -m "added readMe.md file to repository""
	"git push"

